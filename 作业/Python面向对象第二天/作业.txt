1.今天的上课代码实现一遍。
2.手抄什么是私有成员？3遍
3.手抄对象什么情况被销毁？3遍
4.手抄继承的特征？3遍
    单
    多层
    多
5.手抄怎么样调用父类上被重写的方法(语法)？10遍
    super().父类方法名([实参1,实参2,..])
6.定义一个文件操作类File，方法有：读取所有内容，读取数据按行返回，写入内容，追加数据。
    有以下方法：
        file.read(文件名)
        file.write(文件名,’内容’)
        file.readlines(文件名)
        file.append(文件名,’内容’)
7.名片管理系统文件版本的完成
8.将名片管理系统 使用面向对象的方式实现一遍 (在函数的基础上)
9.复习 python基础知识 (放假回来就考)
10. 以上的作用 前两的